{
    "INCLUDE": ["models_[SKBENCH_MODELS_TEMPLATE=test].json", "implem.json"],
    "TEMPLATES": {
        "linear": {
            "SETS": ["linear", "sklearnex[gpu]"]
        },
        "linear array-API": {
            "SETS": ["linear array-API", "sklearnex[gpu]"]
        }
    }
}
