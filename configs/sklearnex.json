{
    "INCLUDE": ["models_[SKBENCH_MODELS_TEMPLATE=test].json", "implem.json"],
    "TEMPLATES": {
        "trees": {
            "SETS": ["trees", "sklearnex[cpu]"],
            "algorithm": {
                "estimator_params": [
                    {},
                    {"max_bins": "[SPECIAL_VALUE]n_samples"}
                ]
            }
        },
        "linear": {
            "SETS": ["linear", "sklearnex[cpu,gpu]"]
        },
        "linear array-API": {
            "SETS": ["linear array-API", "sklearnex[cpu,gpu]"]
        },
        "clustering": {
            "SETS": ["clustering", "sklearnex[cpu]"]
        }
    }
}
